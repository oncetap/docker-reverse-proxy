# Authentik configuration
# Generate secrets with the commands from README.md before starting

PG_USER=authentik
PG_DB=authentik

# Authentik version (pin to a specific version or leave as default)
AUTHENTIK_TAG=2026.5

# Error reporting (optional)
# AUTHENTIK_ERROR_REPORTING__ENABLED=true
